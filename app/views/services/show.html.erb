<div class="card">
  <div class="card-body">
    <h1 class="card-title"><%= @service.name %></h1>
    <p class="card-text"><%= @service.description %></p>
    <%= link_to 'Back', services_path, class: "btn btn-secondary" %>
  </div>
</div>
